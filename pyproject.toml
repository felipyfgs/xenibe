[project]
name = "xenibe"
version = "0.1.0"
description = "Forge experiment lab for Xenibe Ebinex research"
requires-python = ">=3.11"
dependencies = [
  "PyYAML>=6.0",
]

[project.scripts]
forge = "forge.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
